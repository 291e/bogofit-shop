type Product = {
  id: number;
  title: string;
  price: number;
  options: string[];
  url: string;
  category: string;
  imageUrl: string;
  storeName: string;
  createdAt: string;
  updatedAt: string;
};

export type { Product };
